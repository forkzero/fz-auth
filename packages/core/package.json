{
  "name": "fz-auth-core",
  "version": "0.1.0",
  "type": "module",
  "description": "Framework-agnostic BFF auth core — OIDC discovery, PKCE, encrypted sessions",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "@types/node": "^22.0.0"
  }
}
